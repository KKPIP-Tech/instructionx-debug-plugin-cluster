# 字符串工具插件 — 产品需求文档

## 概述
提供常用的字符串处理工具，如统计、反转、替换等操作。

## 功能需求
- **字符串统计**：字符数、单词数、行数统计。
- **字符串反转**：整段文本反转或按单词反转。
- **查找替换**：支持简单文本替换。
- **Base64 编解码**：字符串与 Base64 互转。

## 非功能需求
- 支持 Unicode 字符统计。
- 大文本（>10KB）操作不卡顿。

## 依赖
- `core.plugin.plugin_interface.IPlugin`
- `PySide6.QtWidgets`
